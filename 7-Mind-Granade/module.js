const num1 = 5;
const num2 = 10;

// Function Declare
function addValue(){
    console.log(`sum is ${num1+num2}`);
}

// Function Invoke
addValue()

// after function invoke, I can access it anywhere without assign it to a variable